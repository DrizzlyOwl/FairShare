# FairShare TODOs

- [x] Fix selector mismatch in `UIManager.js` (`.header-brand__logo` -> `.header-brand__icon`).
- [x] Refactor `UIManager.js` to use BEM modifiers for `header-brand` margin instead of direct style manipulation.
- [x] Refactor global typography in `style.css` into BEM-compliant classes (`.type-h2`, `.type-p`, etc.).
- [x] Apply new typography classes to `index.html`.
- [x] Ensure typographic fallbacks are preserved on global element selectors.
- [x] Increment build number and update datetime in `index.html`.
